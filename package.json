{
  "name": "spoke-api-twilio-serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint functions assets",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lint && twilio-run",
    "deploy": "npm run lint && twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.1.3",
    "ramda": "^0.27.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^7.32.0",
    "eslint-config-spoke": "^0.7.0",
    "twilio-run": "^3.2.2"
  },
  "engines": {
    "node": "12"
  }
}
